#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (n); ++i)

int main()
{
    int n;
    cin >> n;

    vector<int> a(n);
    vector<int> b(n);
    rep(i, n) cin >> a.at(i);
    rep(j, n) cin >> b.at(j);

    vector<int> cnt(1000);
}