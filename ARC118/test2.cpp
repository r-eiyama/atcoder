#include <bits/stdc++.h>
using namespace std;

int main()
{
    int K, N, M;
    cin >> K >> N >> M;

    vector<int> A(K);

    for (int i = 0; i < K; i++)
    {
        cin >> A.at(i);
    }

    for (int i = 0; i < K; i++)
    {
        cin >> A.at(i);
    }
}